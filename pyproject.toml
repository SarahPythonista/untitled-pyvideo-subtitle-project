[project]
name = "untitled-pyvideo-subtitle-project"
version = "0.1.0"
description = "Project for subtitling PyVideos"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openai-whisper",
    "yt-dlp",
]
